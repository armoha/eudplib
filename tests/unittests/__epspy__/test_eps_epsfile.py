## NOTE: THIS FILE IS GENERATED BY EPSCRIPT! DO NOT MODITY
from eudplib import *
from eudplib.epscript.helper import _RELIMP, _IGVA, _CGFW, _ARR, _VARR, _SRET, _SV, _ATTW, _ARRW, _ATTC, _ARRC, _L2V, _LVAR, _LSH
# (Line 1) import .test_eps_misc as misc;
# (Line 2) function square();
misc = _RELIMP("", "test_eps_misc")
# (Line 4) const a = [
# (Line 5) square(1),
# (Line 6) square(2),
# (Line 7) square(3),
# (Line 8) square(4),
# (Line 9) square(5)
# (Line 10) ];
a = _CGFW(lambda: [_ARR(FlattenList([f_square(1), f_square(2), f_square(3), f_square(4), f_square(5)]))], 1)
# (Line 12) function square(x) {
@EUDFunc
def f_square(x):
    # (Line 13) const z = EUDArray(5);
    z = EUDArray(5)
    # (Line 14) return x * x; // + z.k;
    EUDReturn(x * x)
    # (Line 15) }
    # (Line 16) const receives = py_eval('[PVariable() for _ in range(8)]');

receives = _CGFW(lambda: [eval('[PVariable() for _ in range(8)]')], 1)
# (Line 17) const attack_gwpID = 4;
attack_gwpID = _CGFW(lambda: [4], 1)
# (Line 18) function constv_thing() {
@EUDFunc
def f_constv_thing():
    # (Line 19) foreach(i, pvar: py_enumerate(receives)) {}
    for i, pvar in enumerate(receives):
        # (Line 20) SetMemoryXEPD(EPD(0x656FB8) + attack_gwpID/4, Add, 100 << (attack_gwpID%4 * 8), 0xFF << (attack_gwpID%4 * 8));  // cooldown +100
        pass

    # (Line 21) return a[0] + a[1] + a[2] + a[3] + a[4];
    DoActions(SetMemoryXEPD(EPD(0x656FB8) + attack_gwpID // 4, Add, _LSH(100,(attack_gwpID % 4 * 8)), _LSH(0xFF,(attack_gwpID % 4 * 8))))
    EUDReturn(a[0] + a[1] + a[2] + a[3] + a[4])
    # (Line 22) }
